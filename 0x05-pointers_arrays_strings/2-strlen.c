#include "holberton.h"

/**
 * _strlen - this function returns the length of a string.
 * @s: is the start position of the pointer.
 * Return: Always 0.
 */
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != '\0'; i++)
{
}
return (i);
}
